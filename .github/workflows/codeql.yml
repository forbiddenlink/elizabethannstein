name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE*'
      - '.gitignore'
      - '.editorconfig'
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'

jobs:
  codeql:
    uses: forbiddenlink/.github/.github/workflows/codeql.yml@7de75a0dd043de01cfda71644f54f48a8d4d07ed # main as of 2026-09-04
    permissions:
      actions: read
      contents: read
      security-events: write
